#!/bin/bash
#
# ~/dev/gh-pages/xlreg_r/generate.gh-pages
#
cd ~/dev/gh-pages/xlreg_r
blackfriday-tool -page  \
    -css style/normalize.css  \
    -css style/main.css \
    index.md index.html
echo "you should check index.html with a browser"
